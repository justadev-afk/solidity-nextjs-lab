{
  "name": "@lab/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Foundry package with the Solidity exercises.",
  "scripts": {
    "test": "forge test -vvv",
    "fmt": "forge fmt",
    "clean": "rm -rf out cache broadcast"
  }
}
